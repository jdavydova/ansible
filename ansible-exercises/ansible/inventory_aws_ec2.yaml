plugin: amazon.aws.aws_ec2

regions:
  - eu-north-1

filters:
  instance-state-name: running
  tag:Name: "dev*"

hostnames:
  - tag:Name

compose:
  ansible_host: public_ip_address
  ansible_user: "'ubuntu'"
  ansible_ssh_private_key_file: "'/Users/juliadavydova/.ssh/julia-key.pem'"

groups:
  app_server: true